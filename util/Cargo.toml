[package]
name = "util"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4"
sqlx = { version = "0.9.0", features = [
    "runtime-tokio",
    "tls-native-tls",
    "postgres",
    "chrono",
    "macros",
    "migrate",
] }
